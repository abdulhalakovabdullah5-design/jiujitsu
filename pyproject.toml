[project]
name = "new-test-project"
version = "0.1.0"
description = ""
authors = [
    {name = "Abdullah8483",email = "abdulkalakovabdullah5@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "new_test_project", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
